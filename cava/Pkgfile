# Description: Console-based Audio Visualizer with support for multiple backends
# URL: https://github.com/karlstav/cava/
# Maintainer: Jay Lanagan, j at lngn dot net
# Depends on: alsa-lib fftw glibc iniparser libpipewire libpulse ncurses portaudio libsdl2 sndio

name=cava
version=0.10.4
release=1
arch=${arch:-$( uname -m )}
source=(https://github.com/karlstav/$name/archive/$version/$name-$version.tar.gz)

build() {
  cd $name-$version
autoreconf -vif
./configure \
  --prefix=/usr \
  --sysconfdir=/etc \
  --localstatedir=/var \
  --disable-debug \
  --enable-static=no \
  --build=$arch-unknown-linux-gnu

make
make install DESTDIR=$PKG

}
