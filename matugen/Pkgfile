# Description: A material you color generation tool with templates
# URL: https://github.com/InioX/matugen/
# Maintainer: Jay Lanagan, j at lngn dot net
# Depends on: clang rust

name=matugen
version=2.4.1
release=1
source=(https://github.com/InioX/$name/archive/v${version}/$name-$version.tar.gz)

build() {
  prt-get isinst sccache && export RUSTC_WRAPPER=/usr/bin/sccache
  mkdir "$PKGMK_SOURCE_DIR/rust" || true
  export CARGO_HOME="$PKGMK_SOURCE_DIR/rust"

  cargo build --manifest-path $name-$version/Cargo.toml --release

  install -Dm755 $name-$version/target/release/$name -t $PKG/usr/bin/

}
